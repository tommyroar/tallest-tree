{
  "name": "dev-server-mcp",
  "version": "1.0.0",
  "description": "MCP server for managing dev servers (Vite, Jupyter) with Tailscale integration",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "bin": {
    "dev-server-mcp": "./index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  }
}
